<p id="notice"><%= notice %></p>

<h1>Contatos</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Endereco</th>
      <th>Email</th>
      <th>Datanascimento</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contatosses.each do |contatoss| %>
      <tr>
        <td><%= contatoss.nome %></td>
        <td><%= contatoss.endereco %></td>
        <td><%= contatoss.email %></td>
        <td><%= contatoss.dataNascimento %></td>
        <td><%= link_to 'Show', contatoss %></td>
        <td><%= link_to 'Edit', edit_contatoss_path(contatoss) %></td>
        <td><%= link_to 'Destroy', contatoss, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Contatoss', new_contatoss_path %>
